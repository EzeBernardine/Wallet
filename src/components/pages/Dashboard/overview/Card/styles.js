import styled from "styled-components";

export const Styles = styled.div`
  .balance {
    background: linear-gradient(-30deg, #f7f5f3 53%, #fff 53%);

    border-radius: 8px;
    padding: 20px;
    box-shadow: 0 1px 4px 0px hsla(198.3, 45.1%, 10%, 0.19);

    .amount > div:first-child {
      border-radius: 8px;
    }
  }
`;
